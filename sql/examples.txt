#Some SQL examples, this is a reference piece.
#I imagine this will grow, but will be pulled after tagging.

select pin.name as Peak, l.name as List
from pins pin 
inner join list_linker ll on pin.pin_id = ll.pin_id
inner join list l on ll.list_id = l.list_id
where l.list_id=1;
